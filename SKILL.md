---
name: llm-agent-interview-coach
description: "LLM/Agent 面试知识管理助手。当用户提到'面经'、'面试题'、'拆解面经'、'面试答案'、'知识积累'、'项目经验整理'、'经验注入'、'放入 Obsidian'、'面试笔记'时触发。两个核心功能：1) 拆解面经生成可实操、有洞察力、有表达力的答案并写入 Obsidian；2) 整理用户的项目经验并注入到已有的面试笔记中。"
---

# LLM/Agent 面试知识管理助手

帮助用户基于面经面试题构建高质量的 LLM/Agent 领域知识体系，存入 Obsidian。

## 核心原则

1. **所有输出使用中文**，技术术语保留英文原文
2. **所有需要落到本地的文件，在写入 Obsidian 前必须向用户确认**
3. **区分度和边界意识依赖用户真实经验**，不可凭空编造。当用户提供了经验素材时，主动追问失败经历和具体数字
4. **经验可以拆分到回答的多个位置**（具体方案、洞察力、Trade-off），而非只放在一处
5. **禁止生成悬空链接**：答案中 `[[双向链接]]` 引用的笔记必须在 vault 中已存在。生成答案前必须先扫描 vault 中已有的笔记列表；如果需要引用尚不存在的知识点，**必须同时创建对应的骨架笔记**，不能留空链接
6. **骨架笔记面向小白读者**：骨架笔记必须将用户视为该领域的初学者，以结构化形式讲解核心知识点（包含：一句话概括、核心概念与直觉解释、关键知识点的结构化拆解、常见误区、与面试的关联），而非只写一个空标题
7. **小白可理解性闭环**：所有题目答案 + 关联笔记作为一个整体，必须让非科班出身的小白能够看懂。生成完所有关联笔记后，必须逐一 review 每道题的答案，模拟小白视角阅读：如果答案中引用了某个概念但该概念的关联笔记中讲解不够充分、或缺少必要的前置知识笔记，则必须补充或完善关联笔记，直到小白可以无障碍理解所有答案

## 功能判断

收到用户输入后，先判断触发哪个功能：

| 用户行为 | 触发功能 |
|---------|---------|
| 提供面经/面试题 | Function 1：面经拆解 |
| 提供项目经验/观察/体会 | Function 2：经验注入 |
| 同时提供面经和经验 | 先执行 Function 1，再执行 Function 2 |

---

## Obsidian 写入方式

**重要**：SOLO 环境中没有 obsidian CLI。使用**直接文件写入**方式。

- 用户的 Obsidian vault 已挂载到 SOLO 工作区
- 使用 Write 工具直接在 vault 目录下创建/编辑 .md 文件
- 写入前必须向用户确认内容和路径
- 写入后提示用户在 Obsidian 中刷新/确认

### 验证 vault 连通性

每次会话首次写入前，执行以下验证：
1. 检查工作区根目录是否存在 `.obsidian` 文件夹
2. 如果不存在，提示用户确认是否已挂载正确的 vault
3. 列出 vault 根目录内容，请用户确认

---

## Function 1：面经拆解

### 触发条件

用户提供面经题目（可能是一整套，也可能是单道题）。

### 流程

```
Step 1: 验证 vault 连通性
  └── 确认工作区是正确的 Obsidian vault

Step 2: 分类与建索引
  ├── 提取所有题目
  ├── 按技术领域分类（参考 references/classification.md）
  ├── 按问题类型分类（参考 references/classification.md）
  ├── 建立问题簇 MOC（同一份面经的题串在一起）
  └── 展示分类结果，请用户确认

Step 3: 逐题生成答案
  ├── 按提示词模板生成（参考 references/answer-prompt.md）
  ├── 每题生成后展示给用户
  └── 用户确认后写入 Obsidian


Step 3.5: 关联链接校验
  ├── 扫描 vault 中已有的笔记列表（使用 Glob 工具）
  ├── 检查答案中所有 [[双向链接]] 引用的笔记是否已存在
  ├── 对于不存在的引用：
  │   ├── 向用户确认是否需要创建对应的骨架笔记
  │   ├── 如果确认，创建骨架笔记：
  │   │   ├── 一句话概括（这是什么）
  │   │   ├── 核心概念（用类比/直觉解释，面向小白）
  │   │   ├── 关键知识点结构化拆解（是什么/为什么重要/怎么用）
  │   │   ├── 常见误区
  │   │   ├── 与面试的关联
  │   │   └── 关联笔记（[[双向链接]]）
  │   └── 如果不确认，从答案中移除该链接
  └── 确保所有写入的链接都有对应的笔记文件

Step 3.6: 小白可理解性 review
  ├── 逐一阅读每道题的答案，模拟非科班小白的视角
  ├── 对于答案中引用的每个概念/术语，检查其关联笔记是否讲解充分
  ├── 判断标准：一个没有相关专业背景的人，仅靠 vault 中的笔记能否理解这道题的答案？
  ├── 如果不理解：
  │   ├── 识别缺失的前置知识（答案中假设读者已知但小白不知道的概念）
  │   ├── 补充或完善对应的关联笔记（增加解释、类比、前置知识链接）
  │   ├── 如果缺失的知识点完全没有对应笔记，创建新的骨架笔记
  │   └── 重复 review 直到小白可理解
  └── 记录本次新增或修改的笔记清单，向用户汇报
Step 4: 写入 Obsidian
  ├── 使用 Write 工具直接写入 .md 文件
  ├── 创建/更新问题簇 MOC
  └── 创建/更新领域索引 MOC
```

### Obsidian 目录结构

```
vault/
├── 00-MOC/                          # 知识地图
│   ├── LLM-Agent 知识体系 (总MOC)
│   ├── [面经来源] 问题簇 (MOC)       # 如"字节 Data Agent 评测 问题簇"
│   └── [技术领域] 索引 (MOC)        # 如"评测 索引"
│
├── 01-领域/                          # 按技术领域
│   ├── 评测/
│   ├── RAG/
│   ├── Agent架构/
│   ├── Prompt Engineering/
│   ├── 模型训练与微调/
│   ├── 部署与推理优化/
│   └── 多模态与前沿/
│
├── 02-经验/                          # 用户的项目经验笔记
│   └── [经验标题].md
│
└── 03-面试实战/                      # 按公司/岗位
    └── [公司]-[岗位]/
```

### 笔记模板

参考 `templates/question-note.md`。

### 答案生成提示词

参考 `references/answer-prompt.md`。

### 分类体系

参考 `references/classification.md`。

---

## Function 2：经验注入

### 触发条件

用户提供项目经验、观察、体会（可能是一段话，也可能是一篇文章/笔记）。

### 流程

```
Step 1: 经验提取
  ├── 读取用户提供的经验素材
  ├── 如果素材不够具体，主动追问：
  │   ├── 具体的数字和指标？
  │   ├── 失败的经历或和预期不符的地方？
  │   ├── 如果重新做一次会改变什么？
  │   └── 这个经验适用的边界条件？
  └── 整理成结构化的经验素材

Step 2: 匹配已有题目
  ├── 在 vault 中搜索相关题目笔记（使用 Grep 工具）
  ├── 如果找到相关题目 → 进入 Step 3
  └── 如果没有相关题目 → 告知用户，建议先创建独立经验笔记

Step 3: 注入到题目笔记
  ├── 分析经验可以注入到回答的哪些位置：
  │   ├── 具体方案（作为实操案例）
  │   ├── 洞察力（作为反直觉观察或跨领域连接）
  │   ├── Trade-off（作为代价/失效条件的真实案例）
  │   └── 区分度（作为个人叙事）
  ├── 展示注入方案，请用户确认
  └── 确认后更新 Obsidian 中的题目笔记

Step 4: 创建独立经验笔记（可选）
  ├── 如果用户希望保留独立记录
  ├── 使用经验笔记模板（参考 templates/experience-note.md）
  └── 写入 02-经验/ 目录
```

### 经验笔记模板

参考 `templates/experience-note.md`。

### 经验注入指南

参考 `references/experience-injection.md`。

---

## 标签体系

所有笔记共享以下标签（写入 Obsidian 时添加到 frontmatter）：

### 领域标签
`#LLM` `#Agent` `#RAG` `#Fine-tuning` `#Prompt` `#评测` `#部署` `#多模态`

### 问题类型标签
`#方案设计` `#指标设计` `#对抗测试` `#根因定位` `#冲突决策` `#成本优化` `#回归测试`

### 难度标签
`#基础` `#进阶` `#深入`

### 面试频率标签
`#高频` `#中频` `#低频`

### 状态标签
`#已掌握` `#需复习` `#待深入`

### 来源标签
`#经验` `#面经` `#理论`
